#include<iostream>
#include<list>
#include"Vector.h"
#include"Edge.h"
int main()
{
	
	std::list<Edge> edges = std::list<Edge>();
	system("pause"); 
	return 0;
}

void Kruskal(std::list<Edge> edges)
{
	std::list<int> vertices = std::list<int>();
	for (std::list<Edge>::iterator iter = edges.begin(); iter != edges.end(); ++iter)
	{
		if(vertices.iter->first)
		vertices.push_back()

	}
	

}